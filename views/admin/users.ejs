<%- include('../include/header') %>
<div class="container-fluid py-4">
    <div class="row">
        <div class="col-md-2">
            <!-- Admin Sidebar -->
            <div class="list-group">
                <a href="/admin" class="list-group-item list-group-item-action">
                    📊 Dashboard
                </a>
                <a href="/admin/products" class="list-group-item list-group-item-action">
                    📦 Products
                </a>
                <a href="/admin/add-product" class="list-group-item list-group-item-action">
                    ➕ Add Product
                </a>
                <a href="/admin/users" class="list-group-item list-group-item-action active">
                    👥 Users
                </a>
            </div>
        </div>
        
        <div class="col-md-10">
            <h2>User Management</h2>
            
            <div class="card">
                <div class="card-header">
                    <h5>All Users</h5>
                </div>
                <div class="card-body">
                    <% if (users.length === 0) { %>
                        <p>No users found.</p>
                    <% } else { %>
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>Username</th>
                                        <th>Email</th>
                                        <th>Role</th>
                                        <th>Created</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% users.forEach(user => { %>
                                        <tr>
                                            <td><%= user.username %></td>
                                            <td><%= user.email %></td>
                                            <td>
                                                <span class="badge badge-<%= user.role === 'admin' ? 'danger' : user.role === 'product_manager' ? 'warning' : 'secondary' %>">
                                                    <%= user.role.replace('_', ' ').toUpperCase() %>
                                                </span>
                                            </td>
                                            <td><%= user.createdAt ? new Date(user.createdAt).toLocaleDateString() : 'N/A' %></td>
                                            <td>
                                                <% if (user._id.toString() !== userId) { %>
                                                    <form method="POST" action="/admin/users/role" style="display: inline;">
                                                        <input type="hidden" name="userId" value="<%= user._id %>">
                                                        <select name="role" class="form-select form-select-sm d-inline-block" style="width: auto;" onchange="this.form.submit()">
                                                            <option value="customer" <%= user.role === 'customer' ? 'selected' : '' %>>Customer</option>
                                                            <option value="product_manager" <%= user.role === 'product_manager' ? 'selected' : '' %>>Product Manager</option>
                                                            <option value="admin" <%= user.role === 'admin' ? 'selected' : '' %>>Admin</option>
                                                        </select>
                                                    </form>
                                                <% } else { %>
                                                    <span class="text-muted">Current User</span>
                                                <% } %>
                                            </td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                    <% } %>
                </div>
            </div>
            
            <div class="mt-4">
                <div class="row">
                    <div class="col-md-4">
                        <div class="card text-center">
                            <div class="card-body">
                                <h5 class="card-title">Total Users</h5>
                                <h2 class="text-primary"><%= users.length %></h2>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="card text-center">
                            <div class="card-body">
                                <h5 class="card-title">Admins</h5>
                                <h2 class="text-danger"><%= users.filter(u => u.role === 'admin').length %></h2>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="card text-center">
                            <div class="card-body">
                                <h5 class="card-title">Customers</h5>
                                <h2 class="text-success"><%= users.filter(u => u.role === 'customer').length %></h2>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../include/footer') %>